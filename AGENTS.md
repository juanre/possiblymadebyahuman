# Agent Instructions

- Follow `docs/sot.md` for architecture and milestone boundaries.
- Preserve the product promise: this is a content-blind writing-record system, not a human/AI detector.
- Do not add humanness verdicts, confidence scores, badges, or certificate language.
- Keep public records plaintext-free; local replay/test fixtures may contain plaintext only for verification.
- Keep changes scoped to the assigned milestone and include tests/evidence in handoff.
- Do not commit `.aw/`, real `.env*`, `.dev` files, build outputs, dependencies, or secrets.
